export const SpanText = ({children}) => {
  return ( 
    <span className="text-white">
      {children}
    </span> 
  );
}

export const Text = ({children}) => {
  return ( 
    <p>{children}</p>
  );
}
